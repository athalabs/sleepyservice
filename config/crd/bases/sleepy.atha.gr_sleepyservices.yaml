---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: sleepyservices.sleepy.atha.gr
spec:
  group: sleepy.atha.gr
  names:
    categories:
    - all
    kind: SleepyService
    listKind: SleepyServiceList
    plural: sleepyservices
    singular: sleepyservice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SleepyService is the Schema for the sleepyservices API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SleepyServiceSpec defines the desired state of SleepyService.
            properties:
              backendService:
                description: BackendService configuration for the managed backend
                  Service
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the backend Service
                    type: object
                  clusterIP:
                    description: ClusterIP to use (optional, usually left empty for
                      auto-assignment)
                    type: string
                  enabled:
                    default: true
                    description: Enabled controls whether to create the backend Service
                    type: boolean
                  externalIPs:
                    description: ExternalIPs that will route to this Service
                    items:
                      type: string
                    type: array
                  loadBalancerIP:
                    description: LoadBalancerIP for LoadBalancer type Services
                    type: string
                  ports:
                    description: |-
                      Ports to expose on the Service
                      If not specified, will auto-create based on the first container port
                    items:
                      description: ServicePort defines a port to expose on the backend
                        Service
                      properties:
                        name:
                          description: Name of the port (required if multiple ports)
                          type: string
                        nodePort:
                          description: NodePort for NodePort/LoadBalancer type (30000-32767)
                          format: int32
                          type: integer
                        port:
                          description: Port to expose on the Service
                          format: int32
                          type: integer
                        protocol:
                          default: TCP
                          description: Protocol (TCP, UDP, SCTP)
                          enum:
                          - TCP
                          - UDP
                          - SCTP
                          type: string
                        targetPort:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            TargetPort on the pod (can be port number or name)
                            If not specified, defaults to Port
                          x-kubernetes-int-or-string: true
                      required:
                      - port
                      type: object
                    type: array
                  sessionAffinity:
                    description: SessionAffinity (ClientIP or None)
                    enum:
                    - ClientIP
                    - None
                    type: string
                  type:
                    default: ClusterIP
                    description: Type of service (ClusterIP, NodePort, LoadBalancer,
                      ExternalName)
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    - ExternalName
                    type: string
                type: object
              components:
                description: Components to manage, in dependency order
                items:
                  properties:
                    dependsOn:
                      description: DependsOn lists component names that must be ready
                        before this one
                      items:
                        type: string
                      type: array
                    name:
                      description: Name is a unique identifier for this component
                      type: string
                    ref:
                      description: Ref identifies the resource
                      properties:
                        name:
                          description: Name of the resource
                          type: string
                        namespace:
                          description: Namespace, defaults to the SleepyService's
                            namespace
                          type: string
                      required:
                      - name
                      type: object
                    replicas:
                      default: 1
                      description: Replicas to scale to when waking (for Deployment/StatefulSet)
                      format: int32
                      type: integer
                    type:
                      description: 'Type of resource: Deployment, StatefulSet, CNPGCluster'
                      enum:
                      - Deployment
                      - StatefulSet
                      - CNPGCluster
                      type: string
                  required:
                  - name
                  - ref
                  - type
                  type: object
                minItems: 1
                type: array
              idleTimeout:
                default: "0"
                description: IdleTimeout triggers auto-hibernate after no activity
                  (0 = disabled)
                type: string
              wakeTimeout:
                default: 5m
                description: WakeTimeout is how long to wait for the stack to wake
                type: string
            required:
            - components
            type: object
          status:
            description: SleepyServiceStatus defines the observed state of SleepyService.
            properties:
              backendService:
                description: BackendService is the name of the created backend Service
                type: string
              components:
                description: Components status
                items:
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    ready:
                      type: boolean
                  required:
                  - name
                  - ready
                  type: object
                type: array
              conditions:
                description: Conditions for standard k8s status reporting
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              desiredState:
                description: DesiredState is what the proxy wants the service to be
                  (set by proxy)
                enum:
                - Sleeping
                - Waking
                - Awake
                - Hibernating
                - Error
                type: string
              lastActivity:
                description: LastActivity is when the service last received traffic
                  (set by proxy)
                format: date-time
                type: string
              lastTransition:
                description: LastTransition is when the state last changed
                format: date-time
                type: string
              proxyDeployment:
                description: ProxyDeployment is the name of the created proxy
                type: string
              state:
                description: State is the current state of the service (set by controller)
                enum:
                - Sleeping
                - Waking
                - Awake
                - Hibernating
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
